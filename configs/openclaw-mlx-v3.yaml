model: Qwen/Qwen3.5-4B
train: true
data: data/mlx-openclaw-v3
adapter_path: adapters/openclaw-mlx-v3

iters: 800
batch_size: 1
learning_rate: 1e-5
num_layers: 8
max_seq_length: 1024
seed: 42
grad_checkpoint: true

steps_per_report: 10
steps_per_eval: 100
save_every: 200

lora_parameters:
  rank: 8
  dropout: 0.05
  scale: 20.0
