model: Qwen/Qwen3.5-4B
train: true
data: data/mlx-openclaw
adapter_path: adapters/openclaw-mlx-v1

iters: 200
batch_size: 1
learning_rate: 2e-5
num_layers: 8
max_seq_length: 512
seed: 42
grad_checkpoint: true

steps_per_report: 10
steps_per_eval: 50
save_every: 50

lora_parameters:
  rank: 8
  dropout: 0.05
  scale: 20.0
